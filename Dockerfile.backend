FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml README.md ./
COPY ci_lib/ ci_lib/
COPY backend/ backend/

RUN pip install --no-cache-dir "fastapi>=0.110" "uvicorn[standard]>=0.29" "pydantic>=2.0" "numpy>=1.21"

EXPOSE 8000

CMD ["python", "-m", "backend.app"]
